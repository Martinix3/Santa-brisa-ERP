
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permite leer y escribir en cualquier documento solo si el usuario
    // ha iniciado sesión en la aplicación.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
