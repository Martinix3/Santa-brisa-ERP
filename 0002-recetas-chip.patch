*** Begin Patch
*** Update File: src/app/(app)/production/execution/page.tsx
@@
-            <SBCard title="Recetas (BOM)" accent={(SB_COLORS as any).module?.produccion ?? SB_COLORS.primary.teal}>
-              <div className="p-2 space-y-1">
-                {boms.map((b: any) => (
-                  <div
-                    key={b.id}
-                    role="button"
-                    tabIndex={0}
-                    onClick={() => selectBom(b)}
-                    onKeyDown={(e) => (e.key === "Enter" || e.key === " ") && selectBom(b)}
-                    className="rounded-lg p-3 border transition-colors outline-none cursor-pointer hover:bg-zinc-50"
-                    aria-label={`Abrir tablero para ${b.name}`}
-                  >
-                    <p className="font-semibold text-zinc-800">{b.name}</p>
-                    <p className="text-xs text-zinc-500">Receta base</p>
-                  </div>
-                ))}
-              </div>
-            </SBCard>
+            <SBCard title="Recetas (BOM)" accent={(SB_COLORS as any).module?.produccion ?? SB_COLORS.primary.teal}>
+              <div className="p-2 space-y-1">
+                {boms.map((b: any) => (
+                  <div
+                    key={b.id}
+                    role="button"
+                    tabIndex={0}
+                    onClick={() => selectBom(b)}
+                    onKeyDown={(e) => (e.key === "Enter" || e.key === " ") && selectBom(b)}
+                    className="rounded-lg p-3 border transition-colors outline-none cursor-pointer hover:bg-zinc-50"
+                    aria-label={`Abrir tablero para ${b.name}`}
+                  >
+                    <div className="flex items-center justify-between">
+                      <p className="font-semibold text-zinc-800">{b.name}</p>
+                      <span className="text-[11px] px-2 py-0.5 rounded-full border bg-white text-zinc-700">
+                        {b.kind === "ENVASADO" ? "ENVASADO" : "PRODUCCIÓN"}
+                      </span>
+                    </div>
+                    <p className="text-xs text-zinc-500 mt-0.5">UoM base: {b.baseUnit}</p>
+                  </div>
+                ))}
+              </div>
+            </SBCard>
*** End Patch
