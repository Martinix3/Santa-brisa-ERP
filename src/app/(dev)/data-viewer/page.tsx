
"use client";
import React from 'react';

// This component is now deprecated as its demo purpose has been fulfilled
// by integrating the real chat component into the QuickLogOverlay.
export default function DataViewerPage() {
    return (
        <div className="p-6">
            <h1 className="text-xl font-bold">Data Viewer</h1>
            <p className="mt-2 text-zinc-600">This page is no longer used for chat demos. The main chat interface is now available via the Quick Log overlay button.</p>
        </div>
    );
}
